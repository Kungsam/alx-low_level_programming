#include <stdio.h>

/**
  * mod - mod of two integers
  * @a: first int
  * @b: second int
  * Return: finl answer
  */
int mod(int a, int b)
{
	return (a & b);
}

/**
  * mul - multiply two integers
  * @a: first int
  * @b: secod int
  * Return: multiply
  */
int mul(int a, int b)
{
	return (a * b);
}

/**
  * add - add two integers
  * @a: first int
  * @b: secod int
  * Return: addition
  */
int add(int a, int b)
{
	return (a + b);
}

/**
  * sub - subtract two integers
  * @a: first int
  * @b: second int
  * Return: subtracted final result
  */
int sub(int a, int b)
{
	return (a - b);
}

/**
  * div - divide two integers
  * @a: first int
  * @b: secod int
  * Return: dividion result
  */
int div(int a, int b)
{
	return (a / b);
}
